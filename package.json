{
  "name": "keetab",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only KeePass password manager as a Tampermonkey userscript — search, copy, and TOTP from any browser tab",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
